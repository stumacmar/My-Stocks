/**
 * Portfolio module — Phase 6 stub.
 *
 * Exports empty shells so imports don't break during Phases 2–5.
 * Replace with real implementations in Phase 6.
 */

export function getPortfolios() {
  return [];
}

export function addHolding(portfolioId, holding) {
  throw new Error('Portfolio module not yet implemented (Phase 6)');
}

export function removeHolding(portfolioId, holdingId) {
  throw new Error('Portfolio module not yet implemented (Phase 6)');
}
